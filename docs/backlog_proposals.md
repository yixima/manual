# 次版へ持ち越す改善候補（発行担当セッションの控え）

- 2026-09-15（v18自作セッションの実測より）: 一部の Chat 環境では URL 取得ツールが**全文を返さず要約して返す**ため、
  URL 方式ではカード全文の適用が原理的にできない。ブートローダー §4（限界）へ
  「取得が全文を返さない環境では、その旨を申告し、ユーザーにカードのファイル添付を求める」を追記する（BL-10 で反映）。
  なお同セッションの「鏡の応答は生成物」説は誤り（対の不一致はファイルごとのキャッシュ世代ずれ。実測で確認・パージで解消済み）だが、
  「読んでいない全文を『適用した』と言わない」という拒否は関門の正しい作動であり、次版の記録に含める。
- 2026-09-15: clair セッション自作の「v10」6条・v18 自作セッションの条文は提案として受領。
  対外提出物と内部資料の粒度分離・相手方提示数値の検証義務は次版で正規に評価する。

- **§5.6「公開リポジトリに引き継ぎを置かない」の実測件数更新（v63候補）**：2026-09-18、公募案件検索ワークフローセッションが yixima/hojo（公開）の git 履歴に引き継ぎ3本（約190KB）を自己検出（3リポジトリ目）。次版で「2つの別のリポジトリ」→「3つ」に更新し、L2 に記録を追加する。対処（Private化 or 履歴書き換え）はユーザー判断待ち。
